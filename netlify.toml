[build]
  command = "npm run build"
  publish = "build-output"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA fallback — all non-file, non-function routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
